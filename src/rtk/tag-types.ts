export enum tagTypes {
  auth = "auth",
  quote = "quote",
  user = "user",
  // pre
  // profile = "profile",
  // users = "users",
  // category = "category",
  // service = "service",
  // order = "order",
  // portfolio = "portfolio",
}

export const tagTypesList = [
  tagTypes.auth,
  tagTypes.quote,
  tagTypes.user,
  //pre

  // tagTypes.profile,
  // tagTypes.users,
  // tagTypes.category,
  // tagTypes.service,
  // tagTypes.order,
  // tagTypes.portfolio,
];
