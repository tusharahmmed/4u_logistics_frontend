export enum tagTypes {
  auth = "auth",
  quote = "quote",
  user = "user",
  driver = "driver",
  // pre
  // profile = "profile",
  // users = "users",
  // category = "category",
  // service = "service",
  // order = "order",
  // portfolio = "portfolio",
}

export const tagTypesList = [
  tagTypes.auth,
  tagTypes.quote,
  tagTypes.user,
  tagTypes.driver,
  //pre

  // tagTypes.profile,
  // tagTypes.users,
  // tagTypes.category,
  // tagTypes.service,
  // tagTypes.order,
  // tagTypes.portfolio,
];
