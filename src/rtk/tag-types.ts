export enum tagTypes {
  auth = "auth",

  // pre
  // user = "user",
  // profile = "profile",
  // users = "users",
  // category = "category",
  // service = "service",
  // order = "order",
  // portfolio = "portfolio",
}

export const tagTypesList = [
  tagTypes.auth,

  //pre

  // tagTypes.user,
  // tagTypes.profile,
  // tagTypes.users,
  // tagTypes.category,
  // tagTypes.service,
  // tagTypes.order,
  // tagTypes.portfolio,
];
